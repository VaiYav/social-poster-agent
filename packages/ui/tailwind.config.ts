import type { Config } from "tailwindcss";

/**
 * Tailwind CSS v4 compatibility file.
 * In v4 the theme lives in CSS via @theme. This file is reserved for
 * custom utilities, plugins, or project-specific extensions that cannot be
 * expressed cleanly in CSS.
 */
export default {
  plugins: [],
} satisfies Config;
